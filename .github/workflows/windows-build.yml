name: Windows Tray Service CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master", "service-rpc-task" ]

jobs:
  build-tray-suite:
    runs-on: windows-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -S . -B build -G "Visual Studio 17 2022" -A x64

      - name: Build Release
        run: cmake --build build --config Release

      - name: Collect artifacts
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path artifacts | Out-Null
          Copy-Item build/Release/TrayWin32App.exe artifacts/TrayWin32App.exe
          Copy-Item build/Release/WitcherTrayService.exe artifacts/WitcherTrayService.exe

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: WitcherTraySuite-windows-x64
          path: artifacts/*.exe
